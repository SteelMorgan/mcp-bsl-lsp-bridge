# Руководство по форматам путей для MCP-LSP Bridge инструментов

## 🆕 Автоматическое преобразование путей (Docker Path Mapper)

**НОВОЕ**: MCP-LSP Bridge теперь автоматически преобразует пути между хост-системой и Docker контейнером!

### Как это работает

1. **Автоматическое определение режима**: Bridge проверяет переменные окружения и автоматически включает Docker режим
2. **Прозрачное преобразование**: Все пути автоматически преобразуются из хост-формата в контейнер-формат
3. **Безопасность**: Проверка, что пути находятся внутри разрешенных директорий

### Переменные окружения

- `HOST_PROJECTS_ROOT` - корневой каталог на хост-системе (например: `D:\Path\To\Projects`)
- `PROJECTS_ROOT` - корневой каталог в контейнере (например: `/projects`)

### Примеры автоматического преобразования

```json
// Входной запрос (хост-путь)
{
  "workspace_uri": "D:\\Path\\To\\Projects\\temp",
  "query": "D:\\Path\\To\\Projects\\temp\\file.bsl"
}

// Автоматически преобразуется в (контейнер-путь)
{
  "workspace_uri": "/projects/temp",
  "query": "/projects/temp/file.bsl"
}
```

## 📋 Результаты тестирования форматов путей

### ✅ Работающие форматы

#### 1. Текущая директория (относительный путь)
```json
{
  "analysis_type": "workspace_analysis",
  "query": ".",
  "workspace_uri": "."
}
```
**Результат**: ✅ Успешно работает

### ❌ НЕ работающие форматы (для Windows путей с пробелами)

#### 1. Относительный путь к подкаталогу
```json
{
  "workspace_uri": "test-workspace"
}
```
**Ошибка**: `project directory does not exist: test-workspace`

#### 2. Относительный путь с ./
```json
{
  "workspace_uri": "./test-workspace"
}
```
**Ошибка**: `project directory does not exist: ./test-workspace`

#### 3. Абсолютный путь Windows (обратные слэши)
```json
{
  "workspace_uri": "D:\\Path\\To\\mcp-lsp-bridge\\test-workspace"
}
```
**Ошибка**: `project directory does not exist: D:\Path\To\mcp-lsp-bridge\test-workspace`

#### 4. Абсолютный путь Unix (прямые слэши)
```json
{
  "workspace_uri": "D:/Path/To/mcp-lsp-bridge/test-workspace"
}
```
**Ошибка**: `project directory does not exist: D:/Path/To/mcp-lsp-bridge/test-workspace`

#### 5. File URI с тремя слэшами
```json
{
  "workspace_uri": "file:///D:/Path/To/mcp-lsp-bridge/test-workspace"
}
```
**Ошибка**: `project directory does not exist: /D:/Path/To/mcp-lsp-bridge/test-workspace`

#### 6. File URI с URL-кодированием пробелов
```json
{
  "workspace_uri": "file:///D:/Path%20To/mcp-lsp-bridge/test-workspace"
}
```
**Ошибка**: `project directory does not exist: /D:/Path%20To/mcp-lsp-bridge/test-workspace`

## 🐳 Работающее решение через Docker

### Docker контейнер с монтированием томов

Для работы с произвольными путями рекомендуется использовать Docker контейнер:

```bash
# Монтируем корневой каталог проектов
docker run --rm -i \
  -v "D:\Path\To\Projects:/projects:ro" \
  mcp-lsp-bridge-bsl-universal \
  mcp-lsp-bridge
```

### Формат путей в Docker контейнере

#### ✅ Работающий формат для Docker
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "project_analysis",
    "arguments": {
      "analysis_type": "workspace_analysis",
      "query": "/projects/temp",
      "workspace_uri": "file:///projects/temp"
    }
  }
}
```

**Результат**: ✅ Успешно работает с обнаружением языка BSL

### Преимущества Docker решения

1. **Универсальный доступ**: Один раз монтируем корневой каталог проектов
2. **Консистентные пути**: Внутри контейнера всегда одинаковые пути (Unix-стиль)
3. **Безопасность**: Read-only монтирование без копирования файлов
4. **Переносимость**: Одинаковая работа на разных ОС

## 📝 Рекомендации по использованию

### Для локального MCP сервера

**Ограничение**: Локальный MCP сервер на Windows имеет проблемы с путями, содержащими пробелы.

**Рекомендация**: Используйте только текущую директорию:
```json
{
  "workspace_uri": "."
}
```

### Для Docker MCP сервера

**Рекомендация**: Используйте пути внутри контейнера в Unix формате:

```json
{
  "workspace_uri": "file:///projects/<имя_проекта>"
}
```

**Пример команды для тестирования**:
```bash
echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"project_analysis","arguments":{"analysis_type":"workspace_analysis","query":"/projects/temp","workspace_uri":"file:///projects/temp"}}}' | docker run --rm -i -v "D:\Path\To\Projects:/projects:ro" mcp-lsp-bridge-bsl-universal mcp-lsp-bridge
```

## 🔧 Конфигурация для Docker

### Настройка MCP конфигурации

Для работы с Docker контейнером используйте конфигурацию из `mcp_config.docker.test.json`:

```json
{
  "mcpServers": {
    "lsp-bsl-bridge": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "mcp-lsp-bridge-universal",
        "mcp-lsp-bridge"
      ],
      "env": {
        "WORKSPACE_ROOT": "/projects",
        "PROJECTS_ROOT": "/projects",
        "HOST_PROJECTS_ROOT": "D:\\Path\\To\\Projects"
      }
    }
  }
}
```

### Монтирование томов

```bash
-v "D:\Path\To\Projects:/projects:ro"
-v "D:\Path\To\Projects:/workspace:ro"
-v "D:\Path\To\Projects:/home/user/projects:ro"
```

## 🎯 Выводы

1. **Для работы с произвольными путями на Windows используйте Docker контейнер**
2. **Внутри контейнера используйте Unix-стиль пути**: `/projects/<путь>`
3. **Для локального MCP сервера используйте только текущую директорию**: `.`
4. **Формат URI внутри контейнера**: `file:///projects/<путь>`

## 🔧 Конфигурация Docker Path Mapper

### Переменные окружения для Docker

```bash
# В Dockerfile.universal
ENV HOST_PROJECTS_ROOT=""
ENV PROJECTS_ROOT="/projects"

# При запуске контейнера
-e "HOST_PROJECTS_ROOT=D:\Path\To\Projects"
-e "PROJECTS_ROOT=/projects"
```

### Автоматическое определение режима

- **Docker режим**: Если `HOST_PROJECTS_ROOT` установлена → автоматическое преобразование путей
- **Локальный режим**: Если `HOST_PROJECTS_ROOT` не установлена → работа без преобразования

### Логирование

Bridge автоматически логирует режим работы:
```
INFO: Docker path mapping enabled: D:\Path\To\Projects -> /projects
WARN: Path mapper initialization failed, using local mode: ...
```

## 📚 Дополнительные ресурсы

- [DOCKER_UNIVERSAL_SETUP.md](DOCKER_UNIVERSAL_SETUP.md) - Универсальная настройка Docker
- [mcp_config.docker.test.json](mcp_config.docker.test.json) - Конфигурация для Docker
